const age = 37
if (age > 17) {
    document.write (mag die persoon naar binnen)
    }

else {
    document.write (blijf alsjeblieft buiten wachten)
}
console.log ("age:", age);


const isFemale = true
if (isFemale) {
    document.write (mag die persoon naar binnen)
} else {
    document.write (mag die persoon niet naar binnen)
}
console.log ("isFemale:", isFemale);
console.log ("false:", false);

const driverStatus = bob
if (bob) {
    document.write (mag die persoon rijden)
}
console.log ("driverStatus:", driverStatus)
console.log ("notBob:", notBob)